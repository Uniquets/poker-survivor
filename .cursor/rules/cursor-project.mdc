---
description: 文档优先级、交付标准、模块边界、牌逻辑不变量、Godot 约定（合并）
alwaysApply: true
---

# 文档权威顺序

实现须按下述顺序服从文档；冲突时以前者优先。代码与文档矛盾时，先修订文档再改实现。

1. `docs/详细设计.md` — **主权威**：系统与模块（第一部）、完全体内容规格（第二部）；后续开发以本文为准  
2. `docs/工程规范.md` — 代码约定、工程边界、Agent 交付  
3. `docs/测试与进度.md` — 任务、冒烟、用例（验证用；玩法与内容以第 1 条为准）  
4. `docs/RULES_V0_2.md` — **仅作历史快照**（旧 MVP 条文），**不再作为实现依据**；与代码或详细设计冲突时忽略本文件  

不得臆造与 `详细设计.md` 矛盾的组牌规则、花色触发或循环时序。

---

# 实现完成标准

每次实现类回复须包含：**已改文件列表**、**实现要点（≤5 条）**、**自动验证结果**、**手动验证步骤**、**风险或后续项**。

回归测试：每次修改完代码后，对于设计的功能，都需要使用godot mcp进行模拟操作验证【使用godot mcp相关指令】，打开场景，模拟操作，直到跑通相关流程，向我汇报自动验证结果，验证失败就分析错误继续修改【无论是测试还是查数据修bug，在需要时使用godot mcp里的指令】。
质量门槛：所改脚本无解析/运行时错误；每次变更后项目能在 Godot Mcp中打开并跑通主流程；实现须符合 `docs/详细设计.md`；非明确要求不做无关重构；不擅自改 `addons/` 与插件/自动加载配置。

## Godot MCP 与验证（硬性）

- **每次修改逻辑代码必备环节**：对 `scripts/cards/`、`scripts/combat/`、Autoload 玩法/战斗相关脚本等**逻辑实现**的变更，在 **Godot MCP Pro**（`user-godot-mcp-pro`）可用时，交付前**必须**完成本节所述 MCP 验证（含 `execute_game_script` / `run_test_scenario` / 必要时的模拟输入），并在实现类回复中写明**自动验证结果**；不可用时的豁免见下文。  
- **H 菜单快测 ↔ MCP 等价（必做其一，战斗/同点数相关时）**：`RunScene` 已勾选 `enable_test_functions` 时，须在战中用 **H** 打开测试菜单，通过「自选花色 + 点数」快速凑牌（如点数 **2** 的单张/对子/三条/四条）做**冒烟**；若使用 Agent，可用 **Godot MCP Pro** 的 `execute_game_script` 等价调用 `RunScene._on_test_menu_add_custom` 或直接用 `PlayEffectResolver` 构造牌组断言。**二者至少完成其一**，并在**自动验证结果**中写明；同时须**检查编辑器报错**（`get_editor_errors` 或面板）。详见 `docs/测试与进度.md`「H 菜单与 MCP 快测」。  
- 当工作区已配置 **Godot MCP PRO**（如 `user-godot-mcp-pro`）且当前会话可调用的 MCP 工具列表中出现对应 Godot 能力时：对本仓库 Godot 工程做**实现类**改动（脚本、场景、资源、与运行强相关的配置）后，**必须**通过 Godot MCP 做验证；**不得**仅以「本机未检测到 `godot` 命令行」等理由跳过 MCP 验证。  
- **验证范围对准本次改动**：优先覆盖**本次修改所影响的功能路径**（例如改了选牌则测选牌与相关 UI；改了战斗则测进入战斗与相关系统），而不是只做与改动无关的泛检查。  
- **交互验证优先、禁止「只启动看一眼」**：在 MCP 支持的前提下，对依赖输入/界面反馈的改动，须使用 **`simulate_mouse_click`、`simulate_sequence`、`simulate_key`、`simulate_action`** 等模拟输入，并结合 **`execute_game_script`**（读节点状态、全局矩形、计数等）或 **`get_game_scene_tree` / `find_ui_elements`** 做断言；**禁止**将「仅 `play_scene` 启动主场景、不做任何模拟操作、不读运行态结论」作为唯一验证。必要时先用脚本计算控件 **`get_global_rect()`** 中心再点击，避免坐标拍脑袋。  
- **测完必须关运行**：使用 `play_scene` 或等价方式启动游戏后，验证结束须调用 **`stop_scene`**（或 MCP 提供的等价「停止运行」），**不得**在无人值守情况下长时间保持编辑器内游戏运行。  
- 若 Godot MCP **未配置、未连接或工具不可用**：须在实现类回复的 **风险或后续项** 中写明原因，并保留 **手动验证步骤**（在 Godot 编辑器内由人执行，且说明如何覆盖**本次改动**的交互路径）；仍须满足上文「无解析/运行时错误、能打开并跑通主流程」的质量门槛。  
- 与 `docs/工程规范.md` 第二节一致：未经明确要求，不修改 `addons/`、`project.godot` 中的插件与 MCP 相关配置。

---

# 玩法模块边界

- UI 脚本不写玩法判定。  
- `scripts/cards/*`：牌数据、组牌检测、牌驱动效果。  
- `scripts/combat/*`：消费已解析效果；不内嵌组牌/牌型规则判定。  
- `scripts/ui/*`：读状态、发用户意图；不计算牌组/分组结果。  
- 避免 card、combat 模块直接绑死具体场景 UI 节点。

---

# 卡牌逻辑不变量（与 `docs/详细设计.md` 第一部及工程基线一致）

- 从左到右扫描牌组。  
- 在当前指针处取**最长**合法组。  
- 打出后指针前进该组长度。  
- 同一次遍历中一张牌只能属于一个组。  
- **装配间隔**在本轮**最后一组牌打出后**开始（逻辑步完成即可，与 `CardRuntime` 一致，不等弹道命中）。  
- 顺子：至少 3 张；A 不能当 1；允许 Q-K-A。  
- 本地花色效果仅针对**当前打出的一组**。  
- 全局花色阈值 3/6/9/12 按花色**分别**累计。

---

# Godot 4 与 UI

- 使用 Godot 4 语法与标准库。  
- 常驻界面以场景中 UI 预制体为主：实例化、布局、信号在场景中完成；避免大量代码动态创建。  
- **UI 布局与默认外观以场景所见即所得为准**：锚点、偏移、`custom_minimum_size`、默认 `theme_override_*`（含 `theme_override_constants` 等）优先写在对应 `scenes/ui/*.tscn`（或子预制体）中；**无运行时必要不在脚本里再赋一遍**，避免编辑器与运行时不一致。  
- **允许在代码中改 UI 的情形（须在脚本中简要注释说明）**：① 纯动态创建的控件（列表项、拖拽浮层、排序条槽位等）；② 随玩法/状态变化的数据绑定（如 `ProgressBar.value`、`Label.text`）；③ 随状态必须变化的样式（如血条按血量改填充色）。禁止仅为「与场景默认值相同」的重复赋值。  
- `scripts/combat/*` 等非 UI 模块对 HUD 节点**避免**设置布局类属性；仅发数据或调用 UI 已暴露的方法。  
- **中文注释（强制）**  
  - **每个函数、方法**（含 `func`、`_ready` / `_process` 等虚拟回调、`static func`、信号槽）：须在函数体前或紧邻声明处用**中文**写清职责、关键参数/返回值、副作用或与玩法/文档的对应关系。  
  - **全局变量与脚本/类作用域字段**：Autoload 单例成员、以及任意脚本中**非函数体内**声明的 `var` / `const`（含类内成员字段）：均须有**中文**注释说明含义、单位或约束。  
  - **函数体内局部变量**不强制逐条注释；复杂分支、状态机、魔法数、协议顺序等仍须用中文点明「为何」与约束。  
  - **逻辑分支处加强注释（强制）**：凡 `if` / `elif` / `else`、`match` 各分支、多路 `return` 等形成**互斥或顺序依赖**的分支，须在分支前或分支首行用**中文**写清：条件对应的业务/玩法语义、为何走该路径、与相邻分支的边界或取舍；禁止仅堆叠条件而无说明的「裸分支」（条件已自解释的一行早退可仅一句简注）。  
  - 注释应简短有信息，勿复述代码字面，勿堆砌无意义废话。
