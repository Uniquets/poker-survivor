extends ShapeEffectSpec
class_name ShapeExplosiveVolleyEffectSpec
## 牌型表占位：**扇形爆炸载荷**（**`EXPLOSIVE_VOLLEY`**）。无表内数值字段；**`PlayShapeEffectAssembler`** 按 **`ctx.cards`** 张数与 **`GameConfig.COMBAT_MECHANICS`** 合成伤害、半径、扇角与灼地参数（与旧解析器公式一致，**不**绑定点数语义）。
